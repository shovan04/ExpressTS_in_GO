get project details from user (Y)
check and validate the inserted data (Y)

layered folder structure (in progress)
    - init (Y)
    - create project folder (Y)
    - create necessary sub folders under src directory (Y) 
    - create package.json tsconfig.json and .env (Y)
    - created all the required data files (Y)
    - implemented all the data files (Y)
    - Update the DTO Validator Middleware (Y)
    - Update the ValidateDto middleware (Y)

DDD Architecture (Y)
    - add exceptions folder to domain layer (Y)
    - add exception, dto, and middleware files to scaffolding (Y)
    - remove try-catch block from user controller (Y)
    - add validation middleware and error handling to user routes (Y)
    - add conflict exception (Y)
    - add error response DTO (Y)
    - add global error handler middleware (Y)
    - add generic response DTO (Y)
    - add DTO validation middleware (Y)
    - add validation exception response (Y)
    - correct validateDto import path in userRoutes (Y)

MVC Architecture (Y)
    - Plan MVC folder structure and templates (Y)
    - Create templates (Model, Controller, Routes, Middleware, MainApp) (Y)
    - Implement initialization logic in init.go and folders.go (Y)
    - Register MVC option in init.go (Y)
    - Verify with test_init_mvc_architecture (Y)
    - Add DTOs in utilities/dtos (Y)
    - Add Generic Response/Error DTOs and Exceptions in utilities (Y)